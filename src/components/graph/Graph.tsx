
import React from "react"

type GraphProps = {
    graphData : number[][],
    width : number,
    height : number,
    directed : boolean
}

const Graph = ({graphData, width, height, directed} : GraphProps) => {
    graphData
    return (
        <div>

        </div>
    )
}
export default Graph